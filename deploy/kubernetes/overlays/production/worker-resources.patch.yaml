apiVersion: apps/v1
kind: Deployment
metadata:
  name: distillery-worker
spec:
  template:
    spec:
      containers:
        - name: worker
          resources:
            requests: { cpu: "1", memory: "2Gi" }
            limits: { cpu: "4", memory: "8Gi" }
